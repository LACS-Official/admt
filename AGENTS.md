ADMT（玩机管家）项目开发规范与设计指南（优化版）
本文档为 ADMT 玩机管家项目统一强制设计与开发标准，适用于项目所有新增功能开发、页面重构、组件封装、交互优化工作。所有前端 UI/UX 设计、工程编码、交互逻辑必须严格遵循本规范，保证项目整体风格统一、体验一致、质感统一。

---
1. 核心设计理念（强制落地）
整体沿用 Apple 官网、Craft Docs 极简人文工具设计风格，主打专业、克制、高效、现代，杜绝廉价网红风、花哨特效、冗余装饰。
1.1 设计核心原则
- 纯粹克制：聚焦工具核心功能与操作效率，无无效视觉装饰、无多余动画、无冗余文案，界面干净整洁。
- 统一圆角规范：功能卡片统一采用 12px–16px 连续平滑圆角（Squircle），按钮、选项卡等交互控件统一 9999px 胶囊圆角，全程杜绝直角、小碎圆角、圆角不统一问题。
- 物理层级分层：依托深浅底色、极淡细边框区分页面层级，浅色模式依托 #ffffff / #f5f5f7、深色模式依托石墨底色 #161617 分层展示，禁止使用粗边框、高饱和描边、刺眼外发光效果。
- 自适应弹性布局：所有功能面板、容器统一采用 height: 100% 弹性自适应布局，内容区内部自适应滚动，禁止写死固定像素高度、禁止双层嵌套滚动条，保证全场景适配。

---
2. 去 AI 刻板化规范（核心质感标准）
全程规避 AI 模板化、机械化、生硬文案与视觉风格，打造人工精致、专业商用级工具质感。
2.1 文案话术统一标准（严格执行）
摒弃模板化、冗余说教式文案，全部使用简洁、直观、符合工具软件使用习惯的业务话术，具体替换规范如下：
❌ 禁止使用（AI 模板/生硬话术）
✅ 统一使用（专业自然话术）
模拟沙盒、遥测数据
状态调试、实时状态、实时参数
AI 助手深度赋能分析、智能解析
直接展示功能名称+操作动作（运行、搜索、导出、刷新等）
大段说教式免责提示、操作风险长篇警告
精简技术提示，高危操作仅通过弹窗二次确认提示，无冗余文案
生硬专业术语：实体、会话实例、Payload 数据
通俗业务术语：已连接设备、执行命令、输出内容
2.2 视觉与图标规范（强制统一）
- 禁止滥用 Emoji：界面文案、按钮、标题中禁止堆砌各类特效 Emoji（⚡、🔋、🚀、✨ 等），保持界面极简专业。
- 图标统一标准：全部使用 @fluenatui/react-icons 单色矢量图标，固定尺寸 16px / 20px / 24px，图标颜色与文字色系统一适配，无彩色、无渐变图标。
- 特效严格管控：禁止使用彩虹渐变文字、青紫霓虹效果、发光外描边、扫光动画、大面积高饱和色块，全程保持低饱和、高级质感。

---
3. 通用布局与组件标准（开发落地模板）
3.1 顶部导航与分段选择器
统一使用顶部胶囊分段选择器替代多层侧边栏，简化页面层级，同模块下所有子功能（按键、显示、系统、电池等）统一横向排布于顶部。
- 激活态：浅色模式品牌蓝 #0071e3，深色模式品牌蓝 #2997ff
- 选中背景：低饱和底色高亮，无生硬变色、无夸张特效
3.2 Bento 模块化卡片容器
所有功能模块统一封装为独立 Bento 卡片，全局样式统一，无差异化自定义。
- 卡片基础样式：圆角 14px，内边距 16px 18px
- 卡片头部：标配功能标题+微弱辅助说明文案
- 卡片右侧：预留快捷操作位（重置、刷新、设置等功能）
3.3 终端与日志流组件
- 字体统一：采用专业等宽字体族 ui-monospace, "SF Mono", Menlo, Consolas, "Cascadia Code", monospace
- 命令行交互：底部集成紧凑型 ADB/Fastboot 命令行，支持上下箭头切换历史命令
- 日志等级标识：统一单色胶囊标签区分级别（INFO 蓝、WARN 橙、ERROR 红、DEBUG 紫），无多余样式

---
4. 主题色彩令牌规范（全局统一变量）
所有颜色严格使用全局主题变量，禁止硬编码色值，统一深浅模式视觉质感。
4.1 浅色模式
- 页面背景：var(--colorNeutralBackground1)（#ffffff / #f5f5f7）
- 卡片背景：var(--colorNeutralBackground2)
- 高亮/激活色：文字主色 #0071e3，背景高亮 rgba(0, 113, 227, 0.08)
- 边框/分隔线：var(--colorNeutralStroke2)（极淡细边框）
4.2 深色模式
- 页面背景：var(--colorNeutralBackground1)（#1f1f1f / #161617）
- 卡片背景：var(--colorNeutralBackground2)
- 高亮/激活色：文字主色 #2997ff，背景高亮 rgba(41, 151, 255, 0.15)
- 边框/分隔线：var(--colorNeutralStroke2)（极淡细边框）

---
5. 补充强制约束（兜底规则）
- 所有样式、色值、布局必须复用全局规范，禁止个性化自定义样式，保证项目统一性
- 所有交互遵循「简洁、高效、无干扰」原则，无无效动画、无弹窗冗余提示
- 全程规避 AI 生成内容的同质化、模板化问题，保持商用工具的精致、克制、专业质感