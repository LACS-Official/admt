# 检查更新功能测试指南

## 功能概述
实现了基于设计文档的"检查更新"功能，主要特性包括：

1. **API调用**: 调用 `https://api-g.lacs.cc/app/software/id/1` 获取版本信息
2. **版本比较**: 比较API返回的 `currentVersion` 与本地版本
3. **用户界面**: 集成到AboutPanel的"检查更新"按钮
4. **状态反馈**: 加载状态、成功提示、错误处理
5. **更新对话框**: 显示版本对比和下载链接

## 测试步骤

### 1. 启动应用
```bash
npm run tauri dev
```

### 2. 访问设置页面
- 点击侧边栏的"设置"
- 切换到"关于"标签页

### 3. 测试版本检查
1. 点击"检查更新"按钮
2. 观察按钮状态变化（显示加载状态）
3. 等待API响应

### 4. 验证结果显示
- **无更新时**: 显示绿色成功提示"当前已是最新版本"
- **有更新时**: 弹出更新对话框显示版本信息
- **网络错误时**: 显示红色错误提示

### 5. 测试更新对话框（如果有更新）
- 验证当前版本和最新版本显示正确
- 点击"立即下载"按钮跳转到下载页面
- 点击"稍后提醒"关闭对话框

## 技术实现

### 修改的文件
1. **versionService.ts**: 更新API端点和响应解析
2. **AboutPanel.tsx**: 集成版本检查逻辑和UI组件

### 新增功能
- 异步版本检查逻辑
- 加载状态管理
- 成功/错误消息提示
- 更新提示对话框
- 防重复点击保护

### API响应格式
```json
{
  "success": true,
  "data": {
    "currentVersion": "2.0.0",
    "officialWebsite": "https://admt.lacs.cc",
    "latestDownloadUrl": "https://csmt.lacs.cc/"
  }
}
```

## 错误处理
- 网络连接失败
- API响应错误
- 版本格式无效
- 超时处理（10秒）

## 用户体验优化
- 按钮禁用防止重复点击
- 加载状态可视化反馈
- 自动清除提示消息（成功3秒，错误5秒）
- 优雅的错误提示

## 注意事项
- 使用设计文档中指定的API端点
- 仅解析 `data.currentVersion` 字段
- 保持与现有UI风格一致
- 确保响应式设计兼容性